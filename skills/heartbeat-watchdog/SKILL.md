# Heartbeat Watchdog

Run this single command:

```bash
bash ~/.openclaw/agents/main/scripts/watchdog-gather.sh
```

The script gathers all metrics, decides on action, auto-restarts if down, and logs to watchdog.log. Report the output line as your response. Nothing else needed.
