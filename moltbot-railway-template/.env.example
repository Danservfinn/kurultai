# =============================================================================
# Moltbot Railway Template - Environment Variables
# OpenClaw Gateway with built-in webchat UI
# =============================================================================

# -----------------------------------------------------------------------------
# OpenClaw Gateway Configuration
# -----------------------------------------------------------------------------

# Node environment
NODE_ENV=production

# OpenClaw gateway port (default: 18789)
OPENCLAW_GATEWAY_PORT=18789

# OpenClaw state directory (persistent config and sessions)
OPENCLAW_STATE_DIR=/data/.openclaw

# Gateway authentication token (REQUIRED for remote access)
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=your_gateway_token_here

# -----------------------------------------------------------------------------
# AI Model Configuration (REQUIRED)
# -----------------------------------------------------------------------------

# Anthropic API key for Claude models
ANTHROPIC_API_KEY=sk-ant-your_key_here

# -----------------------------------------------------------------------------
# Signal Channel Configuration
# -----------------------------------------------------------------------------

# Enable/disable Signal channel
SIGNAL_ENABLED=true

# Signal account phone number (E.164 format)
SIGNAL_ACCOUNT=+1234567890

# Path to Signal data directory
SIGNAL_DATA_DIR=/data/.signal

# Path to signal-cli binary
SIGNAL_CLI_PATH=/usr/local/bin/signal-cli

# Comma-separated list of allowed phone numbers for DMs
SIGNAL_ALLOW_FROM=+1234567890

# Comma-separated list of allowed phone numbers for groups
SIGNAL_GROUP_ALLOW_FROM=

# -----------------------------------------------------------------------------
# Neo4j Connection (for migrations)
# -----------------------------------------------------------------------------

# Neo4j connection URI (use Railway internal networking)
NEO4J_URI=bolt://neo4j.railway.internal:7687

# Neo4j credentials
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_neo4j_password

# -----------------------------------------------------------------------------
# Optional: Monitoring
# -----------------------------------------------------------------------------

# Log level (error, warn, info, debug)
LOG_LEVEL=info
