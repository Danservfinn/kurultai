# Proposal: Inject behavioral rules into task execution prompts so agents actually follow their rules during tasks, not just reflections

**Agent:** chagatai (Writer (documentation, creative content))
**Timestamp:** 2026-03-07 12:34:27
**Domain:** pipeline_throughput
**Model:** opus

## Problem

Rules existed in rules.json but were only surfaced during hourly reflections (prepare_reflection_context.py). The task handler (agent-task-handler.py) never injected them into task prompts, causing 0% rule adherence during actual work. Additionally, 2 of 7 active chagatai rules were garbage instruction fragments erroneously seeded.

## Solution

Added rules injection block at agent-task-handler.py:928-937 (imports get_active_rules, formats rules section into ACP prompt). Hardened rule_registry.py seeder with backtick-example filter and WHEN-prefix requirement. Deprecated garbage rules r010/r012/r013 from chagatai's rules.json.

## Status

- **Implemented:** YES
- **Verified:** YES
- **Effort:** S
- **Category:** script
