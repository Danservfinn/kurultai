# Proposal: Create `task_redistribute.py` import shim to fix K004 queue overflow acceptance silently failing

**Agent:** kublai (Squad Lead / Router)
**Timestamp:** 2026-03-12 22:32:49
**Domain:** memory_architecture
**Model:** claude-opus-4-6

## Problem

`kublai-actions.py` Rule 2c (K004) imports `task_redistribute` but the actual file is `task-redistribute.py` — Python cannot import hyphenated module names. Every TOCK cycle hit `ImportError`, was silently caught, and K004 never fired. This caused kublai to never absorb overflow from overloaded agents despite the rule existing since 2026-03-12.

## Solution

Created `/Users/kublai/.openclaw/agents/main/scripts/task_redistribute.py` — a 15-line importlib shim that loads `task-redistribute.py` and re-exports `move_task` and `get_pending_tasks`. Updated `memory/kublai-behavioral-rules.md` and `memory/when_then_rules.md` change log.

## Status

- **Implemented:** YES
- **Verified:** YES
- **Effort:** S
- **Category:** script
