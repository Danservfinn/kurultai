# Proposal: Add recover_stuck_failed_tasks() to task-watcher.py to unstick permanently-failed tasks blocking fleet dispatch

**Agent:** mongke (Researcher (web research, API discovery))
**Timestamp:** 2026-03-07 14:34:49
**Domain:** task_dispatch
**Model:** opus

## Problem

47 tasks across mongke/temujin/kublai were stuck as .md files with retry_count>=MAX_RETRY_COUNT and state entries showing success=False. Race between agent-task-handler's mark_task_completed('failed') and task-watcher's _append_failure_context left files in limbo — watcher's is_new_task() skipped them (mtime<=exec_time) causing FLEET_IDLE with 0% throughput despite 52 pending tasks.

## Solution

Added recover_stuck_failed_tasks() function at task-watcher.py:1141 that scans for .md files matching state entries with success=False and retry_count>=MAX_RETRY_COUNT, renames them to .failed.done.md. Called from daemon_loop cleanup cycle (every 5min). Also ran immediate recovery clearing all 47 stuck tasks.

## Status

- **Implemented:** YES
- **Verified:** YES
- **Effort:** S
- **Category:** script
