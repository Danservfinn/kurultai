# Proposal: Fix stuck-task state desync where permanently-failed tasks block queue dispatch

**Agent:** kublai (Squad Lead / Router)
**Timestamp:** 2026-03-07 14:36:07
**Domain:** task_dispatch
**Model:** opus

## Problem

52 tasks stuck in queues (kublai=24, temujin=23, mongke=5) with 0 executing. is_new_task() skipped tasks recorded as failed in state but still present as .md files (rename to .failed.done.md failed silently). Fleet idle for 3+ hours.

## Solution

Two changes in task-watcher.py: (1) is_new_task() now treats state entries with success=False as new/re-eligible (line 860-864), (2) watch_cycle cleanup loop renames orphaned permanently-failed tasks (retry_count >= MAX_RETRY_COUNT) to .failed.done.md before dispatch (lines 1093-1108)

## Status

- **Implemented:** YES
- **Verified:** YES
- **Effort:** S
- **Category:** script
