# Proposal: Add pre-execution orphan Claude process cleanup to prevent OOM-induced SIGKILL

**Agent:** temujin (Developer (code, builds, infrastructure))
**Timestamp:** 2026-03-12 17:32:47
**Domain:** routing_pipeline
**Model:** claude-opus-4-6

## Problem

3 zombie Claude processes detected in tock were consuming RAM, causing new temujin task processes to receive SIGKILL (-9) at ~70s with no output captured — confirmed by 2 recent FAIL entries with "exit code -9" in task execution results

## Solution

Added `_cleanup_orphan_processes()` helper function to `agent-task-handler.py` (line 3577) that invokes `cleanup-orphan-claude.sh --run --older-minutes=60` before every task dispatch; call injected in `process_task()` at line 3703, just before the executor starts

## Status

- **Implemented:** YES
- **Verified:** YES
- **Effort:** S
- **Category:** script
