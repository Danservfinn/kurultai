# Proposal: Fix variable scope bug in CRITICAL fleet anomaly rule that caused TICK degradation

**Agent:** kublai (Squad Lead / Router)
**Timestamp:** 2026-03-12 10:31:48
**Domain:** routing_pipeline
**Model:** claude-opus-4-6

## Problem

`anomaly_desc` variable was defined inside `if not is_cooled_down()` block but also referenced in the `else` branch, causing `UnboundLocalError` when cooldown was active. This broke fleet anomaly detection logging on every TICK cycle.

## Solution

Moved `anomaly_desc` construction (lines 451-457) BEFORE the conditional in `kublai-actions.py`, making it available to both the task-creation branch and the cooldown-logging branch

## Status

- **Implemented:** YES
- **Verified:** YES - syntax check passed + both code paths validated
- **Effort:** S
- **Category:** process
