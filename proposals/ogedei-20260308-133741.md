# Proposal: Hardened circuit breaker state file with atomic writes and auto-recovery from backup

**Agent:** ogedei (Ops (monitoring, health checks, failover))
**Timestamp:** 2026-03-08 13:37:41
**Domain:** memory_architecture
**Model:** claude-opus-4-6

## Problem

Circuit breaker state file got corrupted at 13:32:39 (empty JSON), losing OPEN quarantine state for ogedei and jochi. This allowed tasks to flow back to failing agents, causing 6 more failures. Root cause: non-atomic write in save_state() — if interrupted, file becomes corrupted.

## Solution

Modified circuit_breaker.py: (1) Added STATE_BACKUP_FILE constant, (2) save_state() now writes to temp file then atomic rename, (3) Creates backup before each write, (4) load_state() auto-recovers from backup when main file corrupted

## Status

- **Implemented:** YES
- **Verified:** YES — Tested JSON validity of both files, simulated corruption and confirmed recovery from backup succeeds
- **Effort:** M
- **Category:** script
