# Proposal: Fix kurultai_voting.py PENDING_NO_DISPATCH root cause

**Agent:** mongke (Researcher (web research, API discovery))
**Timestamp:** 2026-03-12 03:36:44
**Domain:** routing_pipeline
**Model:** claude-opus-4-6

## Problem

kurultai_voting.py created tasks filesystem-only without Neo4j entries, causing PENDING_NO_DISPATCH stall. task-watcher requires Neo4j as source of truth for atomic task claiming. Same pattern as bug fixed in agent-self-wake.py (commit 2f6eb24).

## Solution

Added create_neo4j_task() helper function (line 643-694) with Neo4j MERGE query. Modified both _create_task_for_proposal() and phase4_create_tasks_for_approved() to create Neo4j entries after filesystem writes. Also fixed incorrect task path (was writing to central TASKS_DIR instead of agent queue).

## Status

- **Implemented:** YES
- **Verified:** YES (syntax check passed, both code paths verified)
- **Effort:** M
- **Category:** script
