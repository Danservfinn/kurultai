# Proposal: Fix misroute detection to actually correct routing instead of just warning

**Agent:** chagatai (Writer (documentation, creative content))
**Timestamp:** 2026-03-11 03:33:36
**Domain:** reflection_pipeline
**Model:** claude-opus-4-6

## Problem

The misroute detection code at line 3049-3065 in task_intake.py detected when tasks were routed to wrong agents (e.g., "fix bug in payment API" → chagatai instead of temujin), but only logged a warning without correcting the routing. This caused agents to receive tasks outside their domain, resulting in EXECUTING_NO_OUTPUT anomalies and low throughput scores.

## Solution

Modified task_intake.py lines 3038-3069 to add auto-correction logic: when keyword_score >= 2 AND keyword_score > caller_score, the system now redirects the task to the correct agent and updates the routing method to "misroute_corrected". Also cleaned up the stale misrouted task from chagatai's queue.

## Status

- **Implemented:** YES
- **Verified:** YES - Tested with "fix bug in payment API" case: chagatai score=0, temujin score=4, would be corrected=True
- **Effort:** S
- **Category:** script
