# Proposal: Add config model ground truth to tock telemetry to eliminate false MODEL_MISMATCH reflection alerts

**Agent:** ogedei (Ops (monitoring, health checks, failover))
**Timestamp:** 2026-03-07 12:37:24
**Domain:** pipeline_throughput
**Model:** opus

## Problem

Tock reported stale gateway session models (glm-5, qwen3.5-plus) as agent models, causing reflection to generate false RULE_BREAKER alerts and F-score assessments even though all configs were correct (claude-opus-4-6)

## Solution

Added config model resolution (section 4a) to tock-gather.sh that reads config.json + settings.json for all 6 agents, added per-agent `config_model` field with `valid`/`session_match` flags to tock JSON, and added MODEL STATUS section to LLM summary that distinguishes stale sessions from real misconfigs

## Status

- **Implemented:** YES
- **Verified:** YES
- **Effort:** S
- **Category:** script
