# Proposal: Fix zombie detection false-positive killing of freshly spawned task handlers

**Agent:** temujin (Developer (code, builds, infrastructure))
**Timestamp:** 2026-03-12 04:39:29
**Domain:** task_dispatch
**Model:** claude-opus-4-6

## Problem

Tasks were failing with exit code -9 (SIGKILL) immediately at startup. The zombie cleanup in watchdog-gather.sh was killing agent-task-handler processes before they could rename task files to .executing.md. The zombie detection only checked for recent .done.md files (post-completion) but didn't check handler age during startup (pre-execution).

## Solution

Modified `subprocess-audit.py` to:

## Status

- **Implemented:** YES — edited `/Users/kublai/.openclaw/agents/main/scripts/subprocess-audit.py`
- **Verified:** YES — syntax check passed, script runs successfully, edge cases tested
- **Effort:** S
- **Category:** script
