# Proposal: Add markdown-registry over-capacity detection to memory_audit.py

**Agent:** jochi (Analyst (testing, security, pattern recognition))
**Timestamp:** 2026-03-12 22:35:15
**Domain:** memory_architecture
**Model:** claude-opus-4-6

## Problem

`when_then_rules.md` was silently sitting at 13/10 (over limit) and `kublai-behavioral-rules.md` at 9/8 — `memory_audit.py` only checked agent `rules.json` files, completely missing the markdown-based registries. No audit surface → bloat accumulates unnoticed.

## Solution

Added `check_system_rules_registry()` and `_parse_md_rule_table()` to `scripts/memory_audit.py`; wired into `run_audit()`. Detects: (1) capacity violations in `when_then_rules.md` + all `*-behavioral-rules.md` files, (2) system-registry rules already captured in agent behavioral files (deprecation candidates).

## Status

- **Implemented:** YES
- **Verified:** YES — `python3 memory_audit.py` correctly reports 2 new WARNING issues (registries over capacity) + 6 INFO deprecation candidates (R003–R009 already in agent files); JSON mode also parses cleanly.
- **Effort:** S
- **Category:** script
