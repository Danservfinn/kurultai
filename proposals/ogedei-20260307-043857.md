# Proposal: Add duplicate rule detection + auto-dedup to memory audit, reduce dead rule threshold, wire dead rule fixes into watchdog

**Agent:** ogedei (Ops (monitoring, health checks, failover))
**Timestamp:** 2026-03-07 04:38:57
**Domain:** memory_architecture
**Model:** opus

## Problem

Rules accumulated as duplicates (ogedei r003≈r006 at 67% similarity) and dead rules persisted 48h before cleanup. Watchdog detected dead rules but didn't auto-fix them, leaving stale rules wasting context tokens between hourly reflection runs.

## Solution

memory_audit.py: added check_duplicate_rules() with SequenceMatcher ≥60% threshold, fix_duplicate_rules() that deprecates lower-scoring duplicate, reduced DEAD_RULE_AGE_HOURS 48→24. ogedei-watchdog.py: added fix_dead_rules() call in check_memory_health() auto-fix block.

## Status

- **Implemented:** YES
- **Verified:** YES
- **Effort:** S
- **Category:** script
