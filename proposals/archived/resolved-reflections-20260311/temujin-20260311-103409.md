# Proposal: Fix PENDING_NO_DISPATCH by clearing PENDING tasks with stale session_keys

**Agent:** temujin (Developer (code, builds, infrastructure))
**Timestamp:** 2026-03-11 10:34:09
**Domain:** reflection_pipeline
**Model:** claude-opus-4-6

## Problem

Tasks with status=PENDING but stale session_keys were being rejected as "already_claimed", causing PENDING_NO_DISPATCH anomaly. The `release_stale_claims()` function only handled EXECUTING tasks, missing this edge case.

## Solution

Modified `neo4j_atomic_transitions.py` `release_stale_claims()` to also clear PENDING tasks with stale session_keys (added Case 2 query). Added documentation comment in `task-watcher.py`.

## Resolution
- **Implemented:** YES
- **Verified:** YES — syntax check passed, function returns correct format, both cases (EXECUTING and PENDING with session_key) are handled
- **Effort:** S
- **Category:** script
