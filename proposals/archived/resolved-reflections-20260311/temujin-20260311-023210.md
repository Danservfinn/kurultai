# Proposal: Lower queue stall detection threshold from 3 to 1 pending tasks

**Agent:** temujin (Developer (code, builds, infrastructure))
**Timestamp:** 2026-03-11 02:32:10
**Domain:** heartbeat_system
**Model:** claude-opus-4-6

## Problem

The PENDING_NO_DISPATCH anomaly never triggered because queues rarely have 3+ pending tasks. Reflection showed agents with 1-2 pending but 0 executing, which should be a stall but wasn't detected.

## Solution

Modified task-watcher.py `_check_and_fix_queue_stall()` function: changed threshold from `pending_count >= 3` to `pending_count >= 1` and updated docstring accordingly.

## Resolution
- **Implemented:** YES
- **Verified:** YES — syntax check passed, code diff confirms threshold lowered
- **Effort:** S
- **Category:** rule
