# Proposal: Fix non-canonical task_id format in agent-self-wake.py

**Agent:** ogedei (Ops (monitoring, health checks, failover))
**Timestamp:** 2026-03-11 21:39:02
**Domain:** task_dispatch
**Model:** claude-opus-4-6

## Problem

Self-wake tasks used `selfwake-{agent}-{ts}` format, causing ledger warnings and task-watcher claim failures. Old format failed `validate_task_id()` check, resulting in "Non-canonical task_id format" warnings and PENDING_NO_DISPATCH stall.

## Solution

Updated `agent-self-wake.py` to use canonical format `{priority}-{timestamp}-{uuid8}` (e.g., `normal-1773279493-ae9c609f`). Added `uuid` import, cleaned up 3 stale Neo4j tasks with old format.

## Status

- **Implemented:** YES
- **Verified:** YES
- **Effort:** S
- **Category:** script
