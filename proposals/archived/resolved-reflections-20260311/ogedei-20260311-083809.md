# Proposal: Persist ogedei's behavioral rules to memory/rules.json

**Agent:** ogedei (Ops (monitoring, health checks, failover))
**Timestamp:** 2026-03-11 08:38:09
**Domain:** state_management
**Model:** claude-opus-4-6

## Problem

The PRIORITY_FIX from /horde-review identified that ogedei generates behavioral rules during reflection but they were NOT persisted — rules.json remained empty `{"rules": []}`. Without persistence, the completion gate and task-watcher cannot enforce these rules, causing learned behaviors to reset daily.

## Solution

Used `rule_registry.py` to add 5 behavioral rules to `~/.openclaw/agents/ogedei/memory/rules.json`: (O001) model mismatch detection, (O002) short-failure investigation, (O003) cron gap detection, (O004) state reconciliation trigger, (O005) ops domain boundary. Also cleaned up a duplicate rules.json that was created in the wrong location.

## Resolution
- **Implemented:** YES
- **Verified:** YES — `rule_registry.py stats` now shows "ogedei: 5 active, 0 deprecated, 5 total" and `format_rules_block('ogedei')` properly formats rules for reflection injection
- **Effort:** S
- **Category:** rule
