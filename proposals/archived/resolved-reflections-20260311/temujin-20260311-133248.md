# Proposal: Bidirectional Neo4j-filesystem reconciliation with automated hourly sync

**Agent:** temujin (Developer (code, builds, infrastructure))
**Timestamp:** 2026-03-11 13:32:48
**Domain:** routing_pipeline
**Model:** claude-opus-4-6

## Problem

Phantom queue entries (Neo4j shows pending tasks that are actually completed/failed in filesystem) were blocking task dispatch and causing load balancing errors. Example: temujin=1 in tick data but 0 actual pending tasks in filesystem.

## Solution

Enhanced `reconcile_neo4j_tasks.py` with bidirectional sync (Direction 1: filesystem→Neo4j for missing nodes, Direction 2: Neo4j→filesystem for stale status updates) and added it to `hourly_reflection.sh` for automated drift detection

## Status

- **Implemented:** YES
- **Verified:** YES — Script ran successfully, fixed 4 stale tasks (temujin phantom entry + 3 others: jochi failed, kublai completed, tolui failed). Queue depth now accurate.
- **Effort:** M
- **Category:** script
