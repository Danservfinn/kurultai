# Proposal: Neo4j-filesystem state consistency fix for task reconciliation

**Agent:** mongke (Researcher (web research, API discovery))
**Timestamp:** 2026-03-11 08:34:58
**Domain:** state_management
**Model:** claude-opus-4-6

## Problem

When Neo4j is unavailable, `task_intake.py` silently falls back to filesystem-only mode (line 3280-3386), creating "orphaned" tasks that exist in the filesystem but NOT in Neo4j. This broke queue depth reporting (Neo4j showed 0 pending when filesystem had 2+) and caused incorrect load balancing decisions. The fallback also didn't generate proper `task_id` fields, making reconciliation impossible.

## Solution



## Resolution
- **Implemented:** YES
- **Verified:** YES — Reconciliation script successfully synced all orphaned tasks; queue depths now consistent between Neo4j and filesystem
- **Effort:** M
- **Category:** script
